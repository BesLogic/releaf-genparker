import { generatedBackendTypes } from './generated-backend-types';

describe('generatedBackendTypes', () => {
  it('should work', () => {
    expect(generatedBackendTypes()).toEqual('generated-backend-types');
  });
});
